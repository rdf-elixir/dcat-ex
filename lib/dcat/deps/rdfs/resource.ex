defmodule DCAT.RDFS.Resource do
  @moduledoc """
  A Grax schema struct for `rdfs:Resource`s.

  The schema is left empty and serves just as a placeholder.
  """

  use Grax.Schema

  alias RDF.NS.RDFS

  schema RDFS.Resource do
  end
end
